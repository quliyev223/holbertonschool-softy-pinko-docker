server {
    # Listen on port 80 for incoming requests
    listen 80;

    # Route all requests to the front-end container
    location / {
        proxy_pass http://front-end:9000;
    }

    # Route /api requests to the back-end container
    location /api {
        proxy_pass http://back-end:5252;
    }
}
